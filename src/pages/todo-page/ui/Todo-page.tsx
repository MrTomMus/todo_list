import { Box } from "@chakra-ui/react"
import { TodoList } from "src/features/Todo-list"

export const TodoPage = () => {

    const hundleButton = ( ) => {
        console.log()
    }

    return (
        <Box>
            <TodoList />
        </Box>
            
    )
}
